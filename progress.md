# 工作進度

2026-09-17：確認位置、讀取適用技能、建立本次計畫文件；官方資料查核與研究進行中。

已完成官方資料查核與專案計畫書（15章），使用者已確認水質與異常風險主軸。文件包含方案比較、資料關卡、避免資料洩漏、時間延遲、驗收、12週時程、資源及風險。

驗證：15個主要章節、主題確認、桃園與高雄直接網址、資料限制、時程驗收、無TODO/TBD占位及無Unicode替代字元，全部通過。另人工檢查時距、門檻與事件量限制一致。僅交付規劃文件，尚未進行資料下載或模型訓練，未提交Git。

2026-09-17追加完成：計畫書更新為v1.1，共16章。新增前端五頁、視覺與響應式要求、JSON契約、GitHub協作、CI／CD、Pages部署、定時更新限制、復原與網站驗收。同步調整12週並行時程、條件式零預算、風險及交付。
驗證：章節連續、前端契約、部署復原、時程整合、移除舊純本機範圍、12個官方來源及編碼檢查全部通過；人工核對Pages不執行Python與批次方案一致。git status確認仍非Git儲存庫；未建庫、提交、推送或部署。此輪只驗證文件，沒有執行網站或模型測試。

2026-09-17：開始依計畫書實作第一個可執行切片。新增 `frontend/` 靜態網站、版本化示範 JSON 快照、純函式資料層、趨勢 SVG、過期提示、事件列表與模型說明；新增 `package.json`、README、資料契約、`.gitignore` 及 `.github/workflows/ci.yml`、`deploy.yml`。示範資料已明確標示，尚未接入官方原始資料。

驗證證據：`npm test` 通過 5 tests、0 failures；本機 `python -m http.server 4173 --directory frontend` 檢查首頁 HTTP 200、module script 存在、manifest HTTP 200 且含 `schema_version` 與 `mode=demo`。HTTP 檢查後已停止伺服器。遠端儲存庫、提交、推送與公開部署尚未執行。

補充驗證：新增「過期快照清空預報值」回歸測試，先觀察到缺少 export 的失敗，再實作 `forecastDisplay`；目前 app 測試與完整 npm test 均通過。四個示範 JSON 已逐一解析通過；HTTP首頁與manifest皆回應200，檢查後已停止伺服器。

2026-09-17：進入第二階段公開資料驗證。查證官方 WQX_P_50 端點為 `https://data.moenv.gov.tw/api/v2/wqx_p_50`，未提供 API key 時官方回傳「api_key 不存在」。資料頁列出的16欄沒有獨立測項名稱或代碼，因此管線禁止自動把 `m_val` 命名為 COD、SS 或 pH。

新增 `pipeline/cwms.py`、`pipeline/validate.py`、Python tests 與管線說明。驗證報告只包含列數、缺欄、錯誤列索引、單位、狀態和測項對照狀態；沒有原始值或金鑰。`python -m unittest discover -s pipeline/tests -p "test_*.py" -v` 通過 5 tests、0 failures。真實資料樣本仍待合法 API key 與官方測項對照，尚未更新前端示範快照。

補充：新增臺灣時區回歸測試，先觀察到 naive timestamp 產生 `+00:00` 的失敗，再修正為 `+08:00`。目前 `npm test` 通過前端5 tests 與 Python 6 tests，總計11 tests、0 failures。真實 WQX_P_50 API 存取仍需合法 `MOENV_API_KEY`，未將任何金鑰寫入專案。

補充：新增「任一資料列缺少必要欄位時必須阻止發布」測試，先觀察到漏報後修正欄位集合累積邏輯。最新整合測試為前端5、Python7，總計12 tests、0 failures。

## 2026-09-17：依使用者原型修訂計畫 v2.0

重寫專案計畫書，主線改為進水四特徵預測處理效率／出水風險，保留前端、GitHub 與分階段部署。舊計畫備份於 docs/archive/專案計畫書-v1.2.md；README 與任務計畫加入新版優先說明。本輪僅修訂文件，未下載資料、重訓模型、修改網站程式或發布服務。

## 2026-09-18：v0.2 四輸入模型實作

- 從 UCI 官方來源取得 Water Treatment Plant 公開資料，確認 527 筆資料列、四項指定進水欄位及 RD-SS-P 目標。
- 完成欄位解析、缺值保留、時間 60/20/20 切分、Ridge 模型、基準比較與 JSON 匯出。
- 完成四輸入互動網站、外推提示、歷史測試案例、模型成效與資料限制呈現。
- 測試集 MAE 10.10，基準 MAE 10.19，改善 0.88%，R² -0.17；未達部署門檻，需補充製程與工業現場資料。
- 本機網站繼續使用 http://127.0.0.1:4173/；尚未建立 GitHub 遠端或公開部署。

## 2026-09-18：審查修正與本機展示

- [x] 首頁入口改為新版四輸入模型殼層，JavaScript 失敗時不再顯示舊版下一小時預報。
- [x] 排除含缺值輸入的測試案例，補上模型產物一致性測試與 GitHub Actions 驗證命令。
- [x] 本機 HTTP 伺服器啟動於 http://127.0.0.1:4173/；入口與模型檔 HTTP 200。
- [ ] 尚未建立遠端 GitHub 儲存庫或公開 Pages 網址。

## 2026-09-18：風險分類切片

- [x] 以 RD-SS-P < 50% 建立研究風險標籤與 Logistic 分類器。
- [x] 風險模型寫入 uci-model.json，測試集 16 個事件、召回率 18.75%、精確率 100%。
- [x] 網站結果卡顯示風險機率與模型成效，低召回率明確標示為研究分析。
- [x] 完整測試 9 個前端、19 個 Python 通過；模型產物一致性驗證通過。

## 2026-09-18：依計畫書重整網站資訊架構

- [x] 對照計畫書五頁需求，改建為總覽、進水評估、歷史驗證、模型成效、資料說明。
- [x] 新增保留測試集實際／預測曲線與案例導向評估。
- [x] 將分類輸出正名為低效率風險分數，不再暗示放流水超標。
- [x] 更新計畫書至 v2.1，誠實記錄已完成與尚未完成項目。
## 2026-09-20：環境部真實申報資料與趨勢圖

- [x] 透過環境部資料頁公開預覽端點擷取最新 5,000 筆 EMS_S_03 明細；原始正規化資料留在 `data/raw/` 並由 Git 忽略。
- [x] 公開摘要只含 COD、SS、pH 的筆數、分位數、申報期間趨勢與化工業中央基準情境比例，不含事業名稱、地址、許可證號、座標或管制編號。
- [x] 情境比較使用 `reference_only_not_compliance` 語意，網站明示跨業別資料不是個別事業法定合規率。
- [x] 前端新增 COD、SS、pH 切換趨勢圖、期間樣本數提示、COD／SS 參考線與 pH 6–9 參考區間。
- [x] 公開 GitHub Pages 與 Google Drive 同步已完成；下一階段需要化工業別標記或實廠逐時資料才能做外部驗證與重訓。
## 2026-09-20：預測區間與超限研究機率

- [x] v4 SS-S 與 DQO-S 模型產物加入時間後段保留測試集殘差，未公開原始測試列。
- [x] 使用殘差的第 10／90 百分位建立 80% 經驗預測區間，並以殘差重抽估計超過化工業參考限值的比例。
- [x] 前端進水預報頁新增 SS／COD 機率條、預測區間、殘差樣本數與模型門檻狀態。
- [x] 缺少殘差時不產生機率；SS 未優於基準時明確標成僅供研究。
- [ ] 待臺灣化工廠逐時資料後重新校準機率、評估 Brier score 與可靠度曲線。
