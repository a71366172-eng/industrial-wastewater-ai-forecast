{
  "name": "wastewater-ai-forecast",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test frontend/tests/*.test.mjs && python -m unittest discover -s pipeline/tests -p \"test_*.py\" -v"
  }
}
